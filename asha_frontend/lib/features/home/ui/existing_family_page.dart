import 'package:flutter/material.dart';

class ExistingFamilyPage extends StatelessWidget {
  const ExistingFamilyPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Select Existing Family'),
      ),
      body: const Center(
        child: Text('Existing Family Page - Placeholder'),
      ),
    );
  }
}
